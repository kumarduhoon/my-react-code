import React from "react"
import Hook from "./Hook"

function App() {
  // const user = "this my code with beer"
  return (
    <>
      <div>Hello React I am back
        {/* {user} this is called experssion and this is evaluate expression, we can not pass the any javscript script code beacuse it except the final result .these html code covert into the object free and tree except the key value not any javascript code 
      // Interpolation: You can also called interpolate JavaScript expressions, variables, or functions into JSX. 
      */}
      </div>
      <Hook />
    </>
  )
}

export default App
